[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Netlify Identity — required for Decap CMS Git Gateway auth
[[plugins]]
  package = "@netlify/plugin-lighthouse"

[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200
